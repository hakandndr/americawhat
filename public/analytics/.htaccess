# Protect log and data files from direct access
<FilesMatch "\.(txt|log|json)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Tracker yalnizca panel icinden — dogrudan erisim kapali
<Files "tracker.php">
    Order Allow,Deny
    Deny from all
</Files>
